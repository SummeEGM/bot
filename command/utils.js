const Discord = require("discord.js");
const fs = require("fs");
const ms = require("ms");

module.exports.run = async (bot, message, args) => {
    //Das was passiert
}

module.exports.help = {
    name: "command_eingeben"
}